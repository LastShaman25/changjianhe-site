"use client";
import ArrowIcon from "@/components/portfolio/ArrowIcon";
import { useState } from 'react';
import type { Locale } from '@/data/projects';
const steps = [['Intake', '导入', 'Review the source structure.', '查看来源结构。'], ['Review', '检查', 'Inspect field roles and sensitivity.', '检查字段角色与敏感性。'], ['Validation', '验证', 'Separate structural preservation, reconstruction resistance, and linkage resistance.', '分别评估结构保持、重建抵抗与关联抵抗。'], ['Approval', '审批', 'A person reviews the evidence and declared capabilities.', '由人审阅证据与声明能力。'], ['Output', '输出', 'Inspect elements and governed artifacts.', '检查计算元素与受治理的产物。']];
export default function StudioDemo({ locale }: {
    locale: Locale;
}) { const [step, setStep] = useState(0), zh = locale === 'zh'; return <div className="studio-demo"><div className="lab-toolbar"><span>INNERFY / STUDIO</span><small>{zh ? '公开演示' : 'Public walkthrough'}</small></div><div className="studio-demo-body"><nav aria-label={zh ? 'Studio 步骤' : 'Studio stages'}>{steps.map((x, i) => <button key={x[0]} aria-pressed={step === i} onClick={() => setStep(i)}>0{i + 1} {x[zh ? 1 : 0]}</button>)}</nav><div aria-live="polite"><span className="large-step">0{step + 1}</span><h3>{steps[step][zh ? 1 : 0]}</h3><p>{steps[step][zh ? 3 : 2]}</p><button className="coral-button" onClick={() => setStep((step + 1) % steps.length)}>{zh ? '下一步' : 'Next stage'} <ArrowIcon direction="right"/></button></div></div><p className="lab-disclosure">{zh ? '根据已实现工作流制作的示意界面。不运行认证或私有变换。' : 'Illustrative interface based on implemented workflows. No certification or private transformation runs here.'}</p></div>; }
